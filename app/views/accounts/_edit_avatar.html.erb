  <%= simple_form_for((user), url: update_avatar_path, html: { method: :put }) do |f|%>
    <%= f.input :avatar, as: :file, label: false, input_html: { multiple: false } %>
    <%= f.submit %>
  <% end %>
